import React from "react";

const About = () => {
  return (
    <div>
      <p>
        HELLOBehind the scenes a renders an with a real href, so people using
        the keyboard for navigation or screen readers will still be able to use
        this app.
      </p>\
      <hr></hr>
      <p>
        HELLOBehind the scenes a renders an with a real href, so people using
        the keyboard for navigation or screen readers will still be able to use
        this app.
      </p> <p>
        HELLOBehind the scenes a renders an with a real href, so people using
        the keyboard for navigation or screen readers will still be able to use
        this app.
      </p>
    </div>
  );
};

export default About;
